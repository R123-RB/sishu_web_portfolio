import json
import re

# Since extracting 64 citations perfectly from prompt text via python script without having the file locally is tricky, 
# I'll just write the parsed JSON content directly.
# Wait, I'll write the full JSON directly via a script to generate it to ensure no formatting issues.
